[pytest]
minversion = 6.0
addopts = --cov=sorcererdb --cov-report=html --cov-report=term
testpaths = tests
markers =
    integration: marks tests as hitting a real database
    unit: marks tests as unit tests (no database)
    slow: marks tests as slow running
